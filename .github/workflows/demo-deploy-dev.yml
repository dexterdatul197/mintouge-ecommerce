name: Deploy Development

on:
  push:
    branches:
      - develop

env:
  AWS_REGION: eu-west-2
  EB_APP_NAME: ecommerce-demo
  EB_ENVIRONMENT: ecommerce-demo-development

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment: development

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Generate deployment package
      run: |
        cp index-develop.html index.html
        zip -r deploy.zip . -x '*.git*' 'dist/*' 'build' 'node_modules/*' '.env*' '.*ignore' 'README.md' 'deploy.zip'

    - name: Beanstalk Deploy
      uses: einaregilsson/beanstalk-deploy@v21
      with:
        aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        region: ${{ env.AWS_REGION }}
        application_name: ${{ env.EB_APP_NAME }}
        environment_name: ${{ env.EB_ENVIRONMENT }}
        version_label: dev_${{ github.run_number }}
        deployment_package: deploy.zip
        use_existing_version_if_available: true
